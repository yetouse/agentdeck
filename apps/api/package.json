{
  "name": "@agentdeck/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight bridge server — REST + SSE event stream for the AgentDeck cockpit",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/claudeTelemetry.test.ts",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.3",
    "typescript": "^5.9.3"
  }
}
